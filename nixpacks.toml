[phases.setup]
nixPkgs = ["nodejs_22"]

[variables]
NODE_OPTIONS = "--max-old-space-size=4096"

[phases.install]
cmds = ["npx -y npm@11.5.1 ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node .output/server/index.mjs"
