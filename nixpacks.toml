[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npx -y npm@11.5.1 ci"]

[phases.build]
cmds = ["NODE_OPTIONS=--max-old-space-size=4096 npm run build"]

[start]
cmd = "node .output/server/index.mjs"
