# Complaint Matrix

Use this file for bugfix sessions driven by user-reported defects.

| ID | Complaint | Repro Steps | Status | Root Cause | Evidence | Files |
| --- | --- | --- | --- | --- | --- | --- |
| CMP-001 | Player can glide through some doors and corridors after the guard-room unlock flow | Collect the key, defeat the enemy, revisit nearby doorways, and move through the corridor edges | resolved | Persistent room-side door-open state removed doorway colliders beyond the intended interaction window | V33 | `src/entities/dungeon/model/floorOneMachine.ts`, `src/widgets/game-canvas/model/useSceneEnvironmentSettings.ts`, `src/entities/room/ui/RoomMesh.tsx` |
| CMP-002 | Key prompt and F action are available in the guard room even when the player is not near the key | Enter guard room before collecting the key and move away from room center | resolved | Interaction resolver previously returned a key candidate from room state alone without proximity gating | V33 | `src/features/dungeon-navigation/lib/doorwayNavigation.ts`, `src/features/dungeon-navigation/lib/interactionResolver.ts` |
| CMP-003 | Transition arrival logic still sometimes lands the player in the middle of the destination room instead of just inside the matching doorway | Traverse through multiple doors and confirm destination placement on entry and backtracking | resolved | Arrival now resolves from canonical doorway anchors, the teleport side effect waits for `lastTransition`, and the player mesh no longer reconsumes room-center spawn props after mount | V36 | `src/entities/player/model/usePlayerMeshViewModel.ts`, `src/widgets/game-canvas/lib/getDoorwayAnchorPosition.ts`, `src/widgets/game-canvas/lib/getDoorwayArrivalPosition.ts`, `src/widgets/game-canvas/model/useGameSideEffects.ts` |
| CMP-004 | World prompts are still not fully aligned with actual interactables | Approach doors, the key, and enemies in multiple camera modes | resolved | Door prompts now use doorway-midpoint anchors and enemy targeting now uses live runtime enemy positions | V36 | `src/shared/lib/enemyPositionStore.ts`, `src/features/dungeon-navigation/model/useInteractionCandidates.ts`, `src/widgets/game-canvas/config/worldInteractionPromptConfig.ts`, `src/widgets/game-canvas/lib/getWorldInteractionPromptPosition.ts`, `src/widgets/game-canvas/ui/WorldInteractionPrompt.tsx` |
| CMP-005 | State authority is split across XState, React effects, and mutable stores | Trace interaction, camera, and side-effect flow from input to transition | pending | Custom stores and imperative orchestration duplicate or bypass machine ownership | V32 | `src/shared/lib/playerPositionStore.ts`, `src/shared/lib/cameraModeStore.ts`, `src/widgets/game-canvas/model/useGameSideEffects.ts` |
| CMP-006 | World interaction hints become too small to read when zoomed out | Show a door, key, or attack prompt, then zoom the active camera out until the hint shrinks noticeably | pending | The world prompt still uses zoom-sensitive HTML scaling with a fixed `distanceFactor` and small fixed font sizes | V39 | `src/widgets/game-canvas/config/worldInteractionPromptConfig.ts`, `src/widgets/game-canvas/ui/WorldInteractionPrompt.tsx` |
| CMP-007 | Free-orbital movement stutters after doorway transitions while the camera is still catching up to the player | Switch to camera mode `4`, open a door with `F`, then continue moving across the threshold while the camera recenters | resolved | The effective fix was treating free-orbital transitions as discontinuities instead of camera-chase movement: freeze movement azimuth during auto-follow, snap transition-sized jumps immediately, and remove ordinary free-orbital chase so the camera no longer spends time catching up through the old room gap. | V42 | `src/widgets/game-canvas/model/useCameraRigViewModel.ts`, `src/widgets/game-canvas/lib/cameraRigFollow.ts` |
| CMP-008 | Player and enemy visuals do not face their travel direction | Move the player or observe enemy patrol/chase behavior and note that the meshes keep a fixed facing direction | pending | Visual yaw is not derived from movement direction for either mesh; rigid-body rotations stay locked and no separate render-facing logic exists yet | V39 | `src/entities/player/ui/PlayerMesh.tsx`, `src/entities/enemy/ui/EnemyMesh.tsx`, `src/entities/player/model/usePlayerMeshViewModel.ts`, `src/entities/enemy/model/useEnemyMeshViewModel.ts` |
| CMP-009 | Guard-room treasury prompt says `Enter Treasury` even when the machine/sidebar still treat the door as locked | Stand at the guard-room south doorway before collecting the key and before defeating the guard | resolved | The interaction resolver previously returned the locked event but kept the success prompt copy, so the world hint disagreed with machine-derived UI state. The prompt now follows the locked event copy. | V42 | `src/features/dungeon-navigation/lib/interactionResolver.ts`, `src/features/dungeon-navigation/lib/interactionResolver.test.ts` |
| CMP-010 | Third-person camera can preserve an obstructed orbit across room transitions, leaving the player or destination room unreadable | Switch to third-person, traverse through a doorway, and observe the camera staying behind the previous wall/doorway geometry | resolved | The effective fix was doorway-aware third-person framing with a clamped temporary arrival-side setback, which keeps the player visible and preserves forward-movement continuity without flipping the camera to the front or placing it inside doorway geometry. | V45 | `src/widgets/game-canvas/model/useCameraRigViewModel.ts`, `src/widgets/game-canvas/lib/cameraRigTargets.ts`, `src/widgets/game-canvas/config/cameraRigConfig.ts` |
