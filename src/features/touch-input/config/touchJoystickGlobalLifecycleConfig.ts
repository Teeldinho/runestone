export const TOUCH_JOYSTICK_GLOBAL_RELEASE_EVENTS = {
	POINTER_UP: "pointerup",
	POINTER_CANCEL: "pointercancel",
} as const;

export const TOUCH_JOYSTICK_GLOBAL_RELEASE_LISTENER_OPTIONS = {
	CAPTURE: { capture: true } as const,
} as const;
