import type { DungeonContext } from "@/entities/dungeon";

export const hasReachedLibrary = (_ctx: DungeonContext): boolean => false;

export const hasCollectedKey = (_ctx: DungeonContext): boolean => false;

export const hasDefeatedAllEnemies = (_ctx: DungeonContext): boolean => false;
